<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.jex01.board.mapper.TimeMapper">

    <select id="getTime2" resultType="string"><!-- 원래는 resultType에 java.lang.String으로 적어야 하지만 통칭 string 으로 가능 -->
        select now()
    </select>

</mapper>

<!-- 자바소스 파일 패키지에 xml 파일을 넣는 것은 흔치 않은 일이다. -->